// Markdown helper — content is passed directly to react-markdown
// No server-side serialization needed since we parse on the client

export function getMarkdownContent(content: string): string {
  return content
}
